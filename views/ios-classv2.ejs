<!DOCTYPE html><html lang="en"><head><meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" /><title>View Codes iOS</title>
    <script defer data-domain="rejectdopamine.com" src="https://t.ldn.jemedia.xyz/js/script.js"></script>
    <style>
    
    body {
              font-family: 'Arial', sans-serif;
              background-color: var(--bg-color);
              color: var(--text-color);
              transition: background-color 0.5s, color 0.5s;
            }
    table {
        margin-top: 20px;
        margin-left: 15px;
        border-collapse: collapse;
      }
    
      th, td {
        padding: 10px;
        border: 1px solid rgb(68, 68, 68);
        text-align: left;
      }
      tr:first-child td:last-child {
          border-top: none;
          border-right: none;
      }
    
      input[type="submit"] {
          background-color: white !important;
          color: black !important;
        padding: 10px;
        border: none;
        border-radius: 4px;
        cursor: pointer;
        transition: background-color 0.3s;
      }
    
      input[type="submit"]:hover {
        background-color: var(--submit-btn-hover-bg-color);
      }
      select {
          background-color: rgb(68, 68, 68);
      }
      input[type="text"], input[type="tel"] {
          background-color: rgb(68, 68, 68);
      }
      .homecontent {
          padding: 15px;
      }
      .sub-table-info {
          padding: 15px;
      }
      .sub-table-link, .sub-table-link:visited {
          color: #58a6ff;
          text-decoration: none;
      }
      .sub-table-link:hover {
          color: rgb(43, 255, 85);
          text-decoration: underline;
      }
      .copy-confirmation {
      position: fixed;
      top: 0;
      left: 50%;
      transform: translateX(-50%);
      padding: 10px;
      background-color: #4CAF50;
      color: white;
      font-weight: bold;
      text-align: center;
      width: 100%;
      box-sizing: border-box;
      z-index: 999;
      opacity: 0;
      transition: opacity 0.5s ease;
      pointer-events: none; /* Allow interactions with elements beneath */
    }
    
    .copy-confirmation.show {
      opacity: 1;
      pointer-events: auto; /* Enable interactions when the confirmation is visible */
    }
    .share-button {
    cursor: pointer;
    padding: 5px 13px;
    background-color: #222;
    color: #fff;
    border: solid;
    border-radius: 4px;
    }
    /* Add some basic styling to the link */
    #fixed-link {
          position: fixed;
          bottom: 20px;
          font-size: 15px;
          right: 20px;
          background-color: #0000003d;
          color: #58a6ff;
          padding: 10px;
          text-decoration: none;
          border-radius: 1px;
          display: none;
      }
      /* Select the table element */
      #data-table {
          /* Add a border around the table */
          border: 1px solid rgb(68, 68, 68);
          /* Add some space between the table and other elements */
          margin: 15px;
        }
      
        /* Select the table cells */
        #data-table td {
          /* Add a border around each cell */
          border: 1px solid rgb(68, 68, 68);
          /* Add some padding inside each cell */
          padding: 5px;
        }
      @media (prefers-color-scheme: dark) {
        body {
          background-color: #0D1117;
          color: #afafaf;
        }
        input[type="submit"] {
          background-color: var(--submit-btn-bg-color);
        color: white;
        }
        .verified-repeatOLD {
          filter: invert(1) sepia(1) saturate(20) hue-rotate(500deg) brightness(0.8);
          }
      }
      
/* temp */
#messageContainer {
  display: none; /* Initially hidden */
  /*width: 100%; /* Span the width of the form */
  margin: 0 auto;
  margin-bottom: 10px;
  padding: 10px;
  border-radius: 5px;
  padding-right: 10px;
  color: #fff; /* Text color */
  text-align: center;
  background-color: #506f4f; /* Light red background for error messages */
}
.hidden {
  display: none !important;
}


      </style>
    </head><body><div id="messageContainer" hidden>While CheckOut is being upgraded to use timetable data, some information may not display correctly. We're working on the fix!<br><br>For complete data please use the <a class="sub-table-link shareLink" href="https://rejectdopamine.com">web version</a>.</div><div class="sub-table-info" id="message-container"><h3 style="padding-left: 15px;">CheckOut codes for <%= moduleName %></h3><table id="data-table"><thead><tr><th></th><th>Check-out Code</th><th style="border-top: none !important;border-right: none !important;"></th></tr></thead><tbody id="table-body"></tbody></table><script><%- include('../views/jquery'); %><%- include('../views/tablecode'); %>
    <div class="sub-table-info"><%- timetableBullets %><br><br><u>Found this service helpful?</u><br>Share the app with coursemates by clicking <a class="sub-table-link shareLink" href="https://rejectdopamine.com"></a> <button class="share-button" onclick="shareLink()">share</button></p><p style="text-align: right;font-size: 10px;"><%- code %>        </p><script>function shareLink(){let e=document.querySelector(".shareLink");navigator.share?navigator.share({title:"The Check-in iOS App",text:"Use this website to share and access the check-out codes.",url:e.href}).then(()=>console.log("Link shared successfully")).catch(e=>console.error("Error sharing link:",e)):alert("Web Share API is not supported on this browser. You can manually copy the link.")}</script></div></div></body></html>