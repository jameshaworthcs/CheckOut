<!DOCTYPE html><html lang="en"><head><title>View Codes</title>
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no" />
    <style>
    
    body {
              font-family: 'Arial', sans-serif;
              background-color: var(--bg-color);
              color: var(--text-color);
              transition: background-color 0.5s, color 0.5s;
            }
    table {
        margin-top: 20px;
        margin-left: 15px;
        border-collapse: collapse;
        margin-bottom: 8px;
      }
    
      th, td {
        padding: 10px;
        border: 1px solid rgb(68, 68, 68);
        text-align: left;
      }
      tr:first-child td:last-child {
          border-top: none;
          border-right: none;
          border-bottom: none;
      }
    
      input[type="submit"] {
          background-color: white !important;
          color: black !important;
        padding: 10px;
        border: none;
        border-radius: 4px;
        /* cursor: pointer; */
        transition: background-color 0.3s;
      }
    
      input[type="submit"]:hover {
        background-color: var(--submit-btn-hover-bg-color);
      }
      select {
          background-color: rgb(68, 68, 68);
      }
      input[type="text"], input[type="tel"] {
          background-color: rgb(68, 68, 68);
      }
      .homecontent {
          padding: 15px;
      }
      .sub-table-info {
          padding: 15px;
      }
      .sub-table-link, .sub-table-link:visited {
          color: #58a6ff;
          text-decoration: none;
      }
      .sub-table-link:hover {
          color: rgb(43, 255, 85);
          text-decoration: underline;
      }
      .copy-confirmation {
      position: fixed;
      top: 0;
      left: 50%;
      transform: translateX(-50%);
      padding: 10px;
      background-color: #4CAF50;
      color: white;
      font-weight: bold;
      text-align: center;
      width: 100%;
      box-sizing: border-box;
      z-index: 999;
      opacity: 0;
      transition: opacity 0.5s ease;
      pointer-events: none; /* Allow interactions with elements beneath */
    }
    
    .copy-confirmation.show {
      opacity: 1;
      pointer-events: auto; /* Enable interactions when the confirmation is visible */
    }
    .share-button {
    cursor: pointer;
    padding: 5px 13px;
    background-color: #222;
    color: #fff;
    border: solid;
    border-radius: 4px;
    }
    /* Add some basic styling to the link */
    #fixed-link {
          position: fixed;
          bottom: 20px;
          font-size: 15px;
          right: 20px;
          background-color: #0000003d;
          color: #58a6ff;
          padding: 10px;
          text-decoration: none;
          border-radius: 1px;
          display: none;
      }
      /* Select the table element */
      #data-table {
          /* Add a border around the table */
          border: 1px solid rgb(68, 68, 68);
          /* Add some space between the table and other elements */
          margin: 15px;
        }
      
        /* Select the table cells */
        #data-table td {
          /* Add a border around each cell */
          border: 1px solid rgb(68, 68, 68);
          /* Add some padding inside each cell */
          padding: 5px;
        }
        #activeClasses {
  justify-content: space-between; /* Distribute space evenly between items */
  margin-bottom: 10px;
  vertical-align: middle;
  margin-left:10px;
  margin-right: 7px;
}

#sessionSelection {
  margin-top: 15px;
  margin-bottom: 15px;
}

.sessionOption {
  flex-grow: 1; /* Allow each option to grow to fill the container */
  padding: 5px 10px;
  margin-right: 5px;
  /* border: 1px solid #ddd; */
  background-color: #181f29;
  border-radius: 5px;
  /* cursor: pointer; */
  transition: background-color 0.3s, color 0.3s;
  margin-bottom: 10px;
  padding: 8px;
  color: white;
}
.sessionInfo {
  color:#b3b3b3;
}
.toprowclass {
    margin-block-start: .5em;
    margin-block-end: 0;
}
.toprowdiv {
    display: flex; justify-content: space-between;
    margin-right: 20px;
    margin-left: 5px;
}
h4 {
    margin-block-start: 0;
    margin-block-end: 0;
    margin-left: 14px;
    font-weight: 100;
}
      @media (prefers-color-scheme: dark) {
        body {
          background-color: #0D1117;
          color: #afafaf;
        }
        input[type="submit"] {
          background-color: var(--submit-btn-bg-color);
        color: white;
        }
        .verified-repeatOLD {
          filter: invert(1) sepia(1) saturate(20) hue-rotate(500deg) brightness(0.8);
          }
      }</style>
    </head><body><div class="sub-table-info" id="message-container"><h3 style="padding-left: 15px;">Current classes:</h3>
        
        <div id="activeClasses">
            <%- sessionOptionsHTML %>
          </div>
        <script>
          function copyText(textToCopy) {
// Create a temporary textarea element to perform the copy
var tempTextarea = document.createElement("textarea");
tempTextarea.value = textToCopy;

// Append the textarea to the document
document.body.appendChild(tempTextarea);

// Select the text inside the textarea
tempTextarea.select();
tempTextarea.setSelectionRange(0, 99999); // For mobile devices

// Execute the copy command
document.execCommand("copy");

// Remove the temporary textarea
document.body.removeChild(tempTextarea);

// Display the copy confirmation message
var copyConfirmation = $('<div class="copy-confirmation">Copied!</div>');
$('body').append(copyConfirmation);

// Add the 'show' class to make the confirmation visible
copyConfirmation.addClass('show');

// Hide the confirmation message after a short delay
setTimeout(function () {
    // Remove the 'show' class to hide the confirmation
    copyConfirmation.removeClass('show');
    
    // Remove the entire element after the transition
    setTimeout(function () {
        copyConfirmation.remove();
    }, 500); // Adjust the delay to match the transition duration
}, 1500); // Adjust the delay as needed
}</script>
        <!-- <table id="data-table"><thead><tr><th></th><th>Check-out Code</th><th style="border-top: none !important;border-right: none !important;"></th></tr></thead><tbody id="table-body"></tbody></table> -->
        
        
        <script><%- include('../views/jquery'); %></script>
        
        <!--  <%- include('../views/sub-table-info'); %> 
        -->
      
      </div></body></html>