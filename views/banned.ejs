<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Banned - CheckOut</title>
    <style>
      :root {
        --bg-primary: #ffffff;
        --bg-secondary: #f8f9fa;
        --text-primary: #2d3436;
        --text-secondary: #636e72;
        --accent-primary: #0984e3;
        --accent-hover: #0769b6;
        --border-color: #dfe6e9;
        --success-bg: #00b894;
        --error-bg: #d63031;
        --container-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1), 0 2px 4px -1px rgba(0, 0, 0, 0.06);
        --link-color: #3498db;
        --link-hover: #2980b9;
        --link-visited: #8e44ad;
      }

      @media (prefers-color-scheme: dark) {
        :root {
          --bg-primary: #1a1b1e;
          --bg-secondary: #2d2e32;
          --text-primary: #ffffff;
          --text-secondary: #a0a0a0;
          --border-color: #404246;
          --container-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.2), 0 2px 4px -1px rgba(0, 0, 0, 0.1);
          --link-color: #60a5fa;
          --link-hover: #93c5fd;
          --link-visited: #c084fc;
        }
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        margin: 0;
        font-family: 'Segoe UI', SegoeUI, 'Helvetica Neue', Helvetica, Arial, sans-serif;
        background-color: var(--bg-primary);
        color: var(--text-primary);
        line-height: 1.6;
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 20px;
      }

      .container {
        width: 100%;
        max-width: 600px;
        background-color: var(--bg-secondary);
        padding: 2.5rem;
        border-radius: 16px;
        box-shadow: var(--container-shadow);
      }

      h1 {
        font-size: 2rem;
        font-weight: 600;
        margin-bottom: 1.5rem;
        text-align: center;
        color: var(--text-primary);
      }

      h2 {
        font-size: 1.5rem;
        font-weight: 500;
        margin: 1.5rem 0;
        color: var(--text-primary);
      }

      hr {
        border: none;
        border-top: 1px solid var(--border-color);
        margin: 1.5rem 0;
      }

      .info-group {
        background-color: var(--bg-primary);
        padding: 1rem;
        border-radius: 8px;
        margin-bottom: 1rem;
      }

      .info-label {
        font-size: 0.875rem;
        color: var(--text-secondary);
        margin-bottom: 0.25rem;
      }

      .info-value {
        font-size: 1rem;
        color: var(--text-primary);
      }

      textarea {
        width: 100%;
        padding: 1rem;
        border: 1px solid var(--border-color);
        border-radius: 8px;
        resize: vertical;
        min-height: 150px;
        background-color: var(--bg-primary);
        color: var(--text-primary);
        font-family: inherit;
        font-size: 1rem;
        transition: border-color 0.2s ease;
      }

      textarea:focus {
        outline: none;
        border-color: var(--accent-primary);
      }

      button {
        background-color: var(--accent-primary);
        color: white;
        padding: 0.75rem 1.5rem;
        border: none;
        border-radius: 8px;
        cursor: pointer;
        font-size: 1rem;
        font-weight: 500;
        width: 100%;
        margin-top: 1rem;
        transition: background-color 0.2s ease;
      }

      button:hover {
        background-color: var(--accent-hover);
      }

      .status-message {
        margin: 1rem 0;
        padding: 1rem;
        border-radius: 8px;
        display: none;
        font-weight: 500;
      }

      .status-success {
        background-color: var(--success-bg);
        color: white;
      }

      .status-error {
        background-color: var(--error-bg);
        color: white;
      }

      .disabled {
        background-color: var(--bg-secondary);
        border-color: var(--border-color);
        color: var(--text-secondary);
        cursor: not-allowed;
      }

      button:disabled {
        opacity: 0.7;
        cursor: not-allowed;
      }

      @media (max-width: 640px) {
        .container {
          padding: 1.5rem;
        }

        h1 {
          font-size: 1.75rem;
        }

        h2 {
          font-size: 1.25rem;
        }
      }

      a {
        color: var(--link-color);
        text-decoration: none;
        position: relative;
        transition: all 0.2s ease;
      }

      a:hover {
        color: var(--link-hover);
      }

      a:visited {
        color: var(--link-visited);
      }

      a:hover::after {
        content: '';
        position: absolute;
        width: 100%;
        height: 1px;
        background: currentColor;
        left: 0;
        bottom: -2px;
        transform: scaleX(1);
        transform-origin: left;
        transition: transform 0.2s ease;
      }

      a:focus {
        outline: 2px solid var(--link-color);
        outline-offset: 2px;
        border-radius: 2px;
      }

      /* Special styling for links within info-group */
      .info-group a {
        font-weight: 500;
      }

      /* For links that should look like buttons */
      .link-button {
        display: inline-block;
        padding: 0.5rem 1rem;
        background-color: var(--accent-primary);
        color: white !important;
        border-radius: 6px;
        font-weight: 500;
        transition: background-color 0.2s ease;
      }

      .link-button:hover {
        background-color: var(--accent-hover);
      }

      .link-button:hover::after {
        display: none;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1>You are banned</h1>
      <hr />

      <div class="info-group">
        <div class="info-label">Reason</div>
        <div class="info-value">
          <%- reason %> <br /><br />
          You may be unable to load the <a href="/data">data access tool</a>, so we encourage you to
          contact <a href="/support">support</a> to make an information request, but do not make an
          appeal except for in the below form.
        </div>
      </div>

      <div class="info-group">
        <div class="info-label">Banned routes</div>
        <div class="info-value"><%= routes %></div>
      </div>

      <div class="info-group">
        <div class="info-label">Expiry</div>
        <div class="info-value"><%= expiry %></div>
      </div>

      <hr />
      <h2>Appeal</h2>
      <div id="appealStatus" class="status-message"></div>
      <form id="appealForm" onsubmit="submitAppeal(event)">
        <textarea id="reason" name="reason" placeholder="Enter your appeal here..."></textarea>
        <button type="submit" id="submitButton">Submit Appeal</button>
      </form>
    </div>

    <script>
      async function submitAppeal(event) {
        event.preventDefault();

        const form = document.getElementById('appealForm');
        const textarea = document.getElementById('reason');
        const button = document.getElementById('submitButton');
        const statusDiv = document.getElementById('appealStatus');

        // Disable form and show loading
        textarea.disabled = true;
        button.disabled = true;
        button.textContent = 'Submitting...';

        try {
          const response = await fetch('/api/app/block/appeal', {
            method: 'POST',
            headers: {
              'Content-Type': 'application/json',
            },
            body: JSON.stringify({ reason: textarea.value }),
          });

          const data = await response.json();

          // Show status message
          statusDiv.style.display = 'block';
          statusDiv.textContent = data.msg;

          if (data.success) {
            statusDiv.className = 'status-message status-success';
            textarea.classList.add('disabled');
            button.style.display = 'none';
          } else {
            statusDiv.className = 'status-message status-error';
            textarea.disabled = false;
            button.disabled = false;
            button.textContent = 'Submit Appeal';
          }
        } catch (error) {
          statusDiv.style.display = 'block';
          statusDiv.className = 'status-message status-error';
          statusDiv.textContent = 'An error occurred while submitting your appeal.';
          textarea.disabled = false;
          button.disabled = false;
          button.textContent = 'Submit Appeal';
        }
      }
    </script>
  </body>
</html>
