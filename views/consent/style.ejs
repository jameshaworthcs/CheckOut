<style>
  body {
    margin: 0;
    padding: 0;
    font-family: Arial, sans-serif;
    font-size: 20px;
    background-color: #000;
  }

  /* For desktop */
  .background {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('https://rejectdopamine.com/static/checkout-cookie-prod-v1.3.2.webp'); /* Change this to your desktop background */
    background-size: cover;
    background-position: center;
    filter: blur(4px);
    z-index: -1;
  }

  /* For mobile */
  @media only screen and (max-width: 600px) {
    .background {
      background-image: url('https://rejectdopamine.com/static/checkout-cookie-prod-mobile-v1.3.2.webp'); /* Change this to your mobile background */
    }
  }

  .content {
    position: absolute;
    top: 50%;
    left: 50%;
    max-width: 40%;
    overflow-y: auto; /* Enable vertical scrolling */
    transform: translate(-50%, -50%);
    background-color: rgba(18, 18, 18, 0.95);
    padding: 20px;
    border-radius: 10px;
    color: #ffffffc4;
    text-align: left;
  }

  /* For mobile */
  @media only screen and (max-width: 1200px) {
    .content {
      width: calc(100% - 70px); /* Adjust padding as needed */
      max-width: calc(100% - 70px);
      left: 50%;
      top: 10%;
      bottom: 10%;
      transform: translate(-50%, 0);
    }
  }

  .content-wrapper {
    margin-bottom: 20px; /* Add margin at the bottom */
  }

  .header {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-bottom: 20px;
  }

  .header img {
    max-height: 50px;
  }

  .headerjem {
    max-height: 30px !important;
  }

  .text {
    margin-bottom: 20px;
  }

  .text {
    margin-bottom: 20px;
    color: #ffffffb9;
  }

  a {
    color: #007bff; /* Link color */
    text-decoration: none;
  }

  a:hover {
    color: #0056b3; /* Hover color */
  }

  .button-container {
    display: flex;
    justify-content: center;
    align-items: center; /* Center vertically */
    margin-top: 20px; /* Add margin to separate from the text */
  }

  .accept-button {
    padding: 10px 20px;
    background-color: #4285f4; /* Dark theme background */
    color: #fff;
    border: none;
    border-radius: 5px;
    cursor: pointer;
    transition: background-color 0.3s;
    font-size: 20px;
  }

  .accept-button:hover {
    background-color: #555; /* Dark theme hover background */
  }

  .learn-more {
    margin-left: 10px;
    color: #999;
    font-size: 14px;
    line-height: 30px; /* Adjust to vertically center */
  }

  /* For mobile */
  @media only screen and (max-width: 600px) {
    .learn-more {
      margin-left: 15 px; /* Center the text */
    }
  }

  .read-more {
    position: fixed;
    left: 50%;
    bottom: 14%; /* Spaced about 20% above the bottom */
    transform: translateX(-50%);
    background-color: #4285f4; /* Dark theme background */
    color: #fff;
    border: none;
    border-radius: 25px; /* Oval shape */
    padding: 10px 20px;
    cursor: pointer;
    z-index: 999; /* Ensure it appears above other content */
    display: none; /* Initially hidden */
    font-size: 25px;
  }

  /* CSS for the button animation */
  .read-more.animation {
    animation: fadeOut 0.5s forwards;
  }

  /* Style for the background pseudo-element */
  .read-more::before {
    content: '';
    position: absolute;
    top: -67px; /* Extends 40px above the button */
    bottom: -40px; /* Extends 40px below the button */
    left: -135px; /* Extends 35px away from the left side */
    right: -135px; /* Extends 35px away from the right side */
    background: linear-gradient(
      to top,
      rgba(0, 0, 0, 1),
      transparent
    ); /* Gradient from dark black to transparent */
    z-index: -1; /* Ensure it's behind the button */
    border-radius: 25px; /* Oval shape */
  }

  @keyframes fadeOut {
    0% {
      opacity: 1;
    }
    100% {
      opacity: 0;
    }
  }
</style>
